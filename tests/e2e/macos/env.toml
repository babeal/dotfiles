# macOS VM connection details. Update here when the VM changes.

[macos]
  vm_name       = "macOS"
  base_snapshot = "Base"
  ip            = "192.168.10.59"
  ssh_user      = "brandt"
  ssh_key       = "~/.ssh/id_ed25519_personal_mac"
